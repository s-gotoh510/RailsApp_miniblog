<!DOCTYPE html>
<html>
  <head>
    <title>RailsApp</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" 
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.css">
    <%= stylesheet_link_tag 'blogs', media: 'all', 
      'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag @blogconfig.stylename, media: 'all', 
      'data-turbolinks-track': 'reload' %>
  </head>

  <body class="container">
    <header id="header">
      <h1 class="display-4 mb-4"><a href="/blogs"><%= @blogconfig.title %></a></h1>
      <h2 class="h4 mb-4"><%= @blogconfig.subtitle %></h2>
    </header>
    <hr>
    
    <div class="clearfix">
      <div id="side">
        <% Bloggenre.all.each do |bloggenre| %>
          <p><a href="/blogs/<%= bloggenre.id %>/genre"><%= bloggenre.name %></a></p>
        <% end %>
      </div>
      
      <main id ="main">
        <%= yield %>
      </main>
    </div>
    
    <div class="mt-5 text-center small text-scondary border-top border-scondary">Copyright My Blog 2021.</div>
    
    <%= javascript_pack_tag 'application', 
      'data-turbolinks-track': 'reload' %>
  </body>
</html>